# Pinned, known-working versions.
# Regenerate with:
#   python -m venv .venv && source .venv/bin/activate  (or .venv\Scripts\activate on Windows)
#   pip install -e .
#   pip freeze | grep -Ei '^(jax|jaxlib|numpyro|matplotlib|numpy|scipy|ml_dtypes|opt_einsum)=='
jax==0.4.35
jaxlib==0.4.35
numpyro==0.15.3
matplotlib==3.9.2
numpy==2.1.3
scipy==1.14.1
ml_dtypes==0.5.0
opt_einsum==3.4.0
